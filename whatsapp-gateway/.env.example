PORT=3000
HOST=127.0.0.1
BAILEYS_GATEWAY_SECRET=change-me-to-a-long-random-string

# Optional: forward inbound WhatsApp messages to Laravel
# LARAVEL_WEBHOOK_URL=https://your-domain.com/api/v1/whatsapp-webhook
# LARAVEL_WEBHOOK_SECRET=same-as-BAILEYS_GATEWAY_SECRET-or-dedicated

# Delivery/read receipts for outbound messages (contact invitations)
# Required: same secret as Laravel BAILEYS_GATEWAY_SECRET
LARAVEL_APP_URL=https://your-domain.com
# Same server as Laravel — prefer loopback (avoids firewall/SSL issues):
# LARAVEL_RECEIPT_INTERNAL_URL=http://127.0.0.1/api/v1/webhooks/baileys-message-status
# If nginx needs vhost: LARAVEL_WEBHOOK_HOST=your-domain.com
# LARAVEL_RECEIPT_WEBHOOK_URL=https://your-domain.com/api/v1/webhooks/baileys-message-status
# RECEIPT_DEBUG=true

SESSIONS_DIR=./sessions

# Browser QR page at GET /sessions/:id/qr/page?token=SECRET (set false after setup)
ENABLE_QR_SETUP_PAGE=true
